# Core training dependencies for this repo
jsonlines==3.1.0
transformers==4.48.3
numpy==1.24.0
pandas>=1.5,<2.3
datasets>=2.14,<3.0
tokenizers>=0.15,<0.22
modelscope>=1.12

# NOTE:
# torch is intentionally not pinned here because Windows users need to pick
# the wheel matching their CUDA/CPU runtime.
